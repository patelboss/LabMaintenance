### 1. Vector & Label Cleanup
 * [ ] **Override Raw Label Text:** Under Layer Properties \rightarrow Labels \rightarrow Expression (\varepsilon), set the label value to 'Block Tankitola 406' directly instead of using the raw attribute field.
 * [ ] **Add Text Halos:** Enable a **1.0 mm white text buffer** for all Munara point numbers (1 to 36) and feature labels (*Sheet rock*, *Open jungle*) to ensure high contrast over contour lines.
### 2. Grid & Coordinates (A3 Precision)
 * [ ] **Adjust Grid Interval to 15":** In Layout Map Grid Properties, set Interval X and Y to **15"** (0.00416667 degrees) for clean Survey of India coordinate spacing.
 * [ ] **Format Border Ticks:** Ensure coordinate annotations display neat, round seconds (15'', 30'', 45'').
### 3. Layout Restructuring & Aesthetics
 * [ ] **Update Frame Styling:** Change the map frame/border line color from grey to solid **black**.
 * [ ] **Reposition Elements:**
   * Shift **Scale Bar** and **North Arrow** slightly downward/inward for cleaner spacing.
   * Move the **Legend** and **Topo Sheet Number Table** to the left panel.
   * Move the **Minimap (Index Map)** to the bottom section.
 * [ ] **Mask Clipped Edges:** Place a small white rectangle shape over the right inner border to hide truncated topo sheet text (like Bh<m).
### 4. Table Polish & Final Export
 * [ ] **Enhance Munara Table:** Make the top-right table font **bold and darker**, increase row padding slightly, and center-align the latitude/longitude numbers.
 * [ ] **Clean Legend Items:** Keep only active vector layers (*Proposed Boundary*, *Munara Points*, *Distance Line*) in the main legend.
 * [ ] **Export:** Save as a high-resolution **PDF at 300 DPI** for final submission.
